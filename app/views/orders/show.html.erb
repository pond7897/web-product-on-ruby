
<div>
    <h1 class="mx-3"> Order Summary </h1>

    <p class="mx-3"> <%= @order.product.name %> </p>
    <p class="mx-3"> <%= @order.product.description %> </p>
    <p class="mx-3"> <%= @order.quantity %> </p>
    <p class="mx-3"> <%= @order.product.price %> </p>
    <p class="mx-3"> <%= @order.total_price %> </p>

    <button class="mx-3 bg-green-400 border-2 border-green-700 rounded-md h-8 w-16"> ชำระเงิน </button>
</div>